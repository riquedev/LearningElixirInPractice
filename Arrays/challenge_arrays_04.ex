defmodule Challenge_Arrays_04 do
  @moduledoc """
  04 - Check if all data in a list are of the same type
  Writes a person that receives an array of data of the same type and that receives all data of the same type.
  If all are compensated:
  ------
  true
  ------
  Otherwise:
  -------
  false
  -------
  Note: Returns must be boolean data.
  """

  @spec data_type(list) :: boolean
  def data_type(types) do
    # Develop your code in this function
  end

end

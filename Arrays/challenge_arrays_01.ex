defmodule Challenge_Arrays_01 do
  @moduledoc """
  01 - Find the largest number in an array
  Write an algorithm that takes an array of integers, looks for the largest value, and returns it.
  """

  @spec get_max_number(list) :: number
  def get_max_number(numbers) do
    # Develop your code in this function
  end

end
